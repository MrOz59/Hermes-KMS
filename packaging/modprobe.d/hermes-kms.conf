# Default Hermes-KMS module options for compositor-driven streaming.
#
# Leave the connector disconnected until a controller claims it. An optional
# seat broker can configure a private session pool separately.
options hermes_kms initial_enabled=0 outputs=1
