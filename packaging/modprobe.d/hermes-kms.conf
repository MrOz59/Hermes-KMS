# Default Hermes-KMS module options for compositor-driven streaming.
#
# Leave connectors disconnected until Hermes assigns one to a stream. Hotplug
# events remain enabled so a compositor can adopt shared-desktop outputs on
# demand. Independent-session cards are owned by their private compositors.
options hermes_kms initial_enabled=0
